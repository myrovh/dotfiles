# {{@@ header() @@}}
[user]
name = myrovh
email = admin@myrovh.dev

[color]
diff = auto
status = auto
branch = auto

[push]
default = current

[pull]
rebase = true

[credential]
helper = store

[core]
autocrlf = input
pager = delta

[interactive]
diffFilter = delta --color-only

[filter "lfs"]
clean = git-lfs clean -- %f
smudge = git-lfs smudge -- %f
process = git-lfs filter-process
required = true

[url "git@bitbucket.org:"]
insteadOf = https://bitbucket.org/
