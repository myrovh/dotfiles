
[user]
	name = myrovh
	email = myrovh@yandex.com
[color]
	diff = auto
	status = auto
	branch = auto
[push]
	default = current
[credential]
helper = store
[core]
	autocrlf = input
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
